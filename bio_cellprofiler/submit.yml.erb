# Job submission configuration file
#
---

batch_connect:
  template: "vnc"

#
# sgd - 11/02/2018
#
script:
  email_on_started: true
  email_on_terminated: true
  email: <%= ENV["USER"] %>@tufts.edu
  job_name: "OnDemand/Cellprofiler"
  native:
    - "--time"
    - "<%= bc_num_hours %>:0:0"
    - "-n"
    - "<%= num_cores %>"
    - "--mem"
    - "<%= num_memory %>"
    - "--nodes"
    - "1"
    - "--reservation"
    - "<%= reservation %>"
